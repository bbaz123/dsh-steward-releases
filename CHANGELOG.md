# 更新记录

本文件记录 DSH 管家的版本变更。更新通道（`manifest.json` + `DSH管家.bin`）与本地交付版本在这里统一登记，
便于判断「我手上的 exe 是哪个版本、更新通道里现在是什么版本」。

格式：`版本号 — 发布日期 — 是否进入更新通道`。

---

## 1.2.1 — 2026-09-23 — 已进入更新通道

**定位升级：从「体检器 + 更新器」变为「故障诊断中心 + 维护中心」。**

新增：

- **诊断链路**：受管启动 / 接管 DSH → 采集真实现场（stdout、stderr、端口、进程树、健康探针）→
  提取证据 → 命中 11 条诊断规则 → 结论必须带证据（输出片段、堆栈帧、端口占用者、文件路径 + 行号）。
- **唯一一条可自动修复的闭环规则** `DSH-JSON-BOM-001`：前置 SHA256 校验 → Recovery 备份 →
  剥离 BOM 3 字节 → Node `JSON.parse` 复验 → 重启 → 健康探针 → 提交或自动回滚。
- **维护隔离**：原「一键修复」降级为维护中心里的「全套维护」，诊断链路不再自动升级任何东西。
- **离线自测**：`--selftest` 在临时沙箱（假 DSH、假 npm）跑 69 项断言。
- 命令行新增 `--diagnose` / `--repair [--yes]` / `--selftest` / `--stop-dsh`。

修复：

- 健康地址缓存串场：旧实例淘汰的地址会污染新实例，导致误判「启动失败」；现在新会话一律作废旧地址，
  且缓存地址必须端口仍在监听才算数。
- 端口误报：管家自己启动的 DSH 正在监听被当成「端口故障」；现在属于正面证据，只有他人占用才提示。
- 多实例认错：机器上存在多个 dsh node 进程时只认第一个会认错；现在先列全部候选，再按进程树 /
  同一安装目录归属判断。
- 管道调用卡死：受管启动的 DSH 继承标准句柄，等于一直攥着调用方的管道写端（实测卡住 497 秒）；
  改用 `CreateProcess`（`bInheritHandles=FALSE`）后同一场景 49 秒正常返回。
- 配置路径越存越长：读配置未还原 JSON 转义，`C:\...` 读出来变成 `C:\\...`，再保存反斜杠翻倍；
  现在按 JSON 规则还原，并补了「存 → 读 → 再存 → 再读」回归断言。
- 自带日志文件头有 BOM：启动时一次性清除历史 BOM，新写日志一律无 BOM。
- 探针每秒刷日志：改为仅在地址或来源变化时记录一行。

兼容性：文件名、配置项、启动方式不变；旧配置文件可直接使用；`--health` 的输出结构与退出码与 1.1.0 一致。

说明文档：[`docs/diagnostics.md`](./docs/diagnostics.md)、[`docs/troubleshooting.md`](./docs/troubleshooting.md)。

## 1.1.0 — 2026-09-19 — 本地交付版（未进入更新通道）

- 重做图形界面：顶部操作栏 + 概览 / 插件管理 / 运行日志三个页面。
- 每个体检项目提供独立的「修复 / 升级」按钮，操作完成后自动重新体检。
- 自动检测读取 web profile 依赖清单，逐项列出插件名称、当前版本、最新版本与更新状态。
- 区分语义：「修复」按当前版本重装，「升级」才安装最新版本。

## 1.0.2 — 2026-09-19 — 已进入更新通道

- 修复「一键修复 / 立即更新」结束后的自动复检被防重入锁吞掉的问题。
- 修复配置 `checkUpdatesOnLaunch: false` 不生效的问题（新增布尔字段解析）。
- 修复预发布版本比较使用字符串排序导致 `rc.10` 被判为小于 `rc.9` 的问题（改为逐段语义化比较）。

## 1.0.1 — 本地修复批次（未作为独立版本进入更新通道）

- 日志每行写两遍；`DSH管家-health.json` 只在命令行模式更新；git 离线时长时间卡住；
  自更新失败原因被吞掉；报告结论自相矛盾；联网查询走 Schannel 连不上 CDN（改用 node/OpenSSL）。

## 1.0.0 — 2026-09-11 — 首个公开版本

- Windows 单文件程序：npm 镜像切换、全局 dsh 检查/安装/升级、插件版本对齐、仓库拉取与构建、
  配套服务启动、插件更新检查、官方 `doctor` 自检。
- 建立 jsDelivr CDN 更新通道（`manifest.json` + `DSH管家.bin`），程序内可自动检查与替换。
- 加固：stdin 重定向、`GIT_TERMINAL_PROMPT`、防重入、全插件检查。
