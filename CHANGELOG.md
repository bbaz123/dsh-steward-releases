# 更新记录

本文件记录 DSH 管家的版本变更。更新通道（`manifest.json` + `DSH管家.bin`）与本地交付版本在这里统一登记，
便于判断「我手上的 exe 是哪个版本、更新通道里现在是什么版本」。

格式：`版本号 — 发布日期 — 是否进入更新通道`。

---

## 1.3.0 — 2026-09-24 — 已进入更新通道

**定位扩展：从「故障诊断中心」扩成「故障诊断 + 数据安全」双中心。**

新增：

- **备份中心**：全量 / 分类备份（`tar.gz` + `.sha256` + `.meta.json` + `.redacted.json`）、逐份校验
  （缺 `.sha256` 一律算「无法确认」）、预演恢复、执行恢复（**校验通过才动手**；现有数据改名挪旁，
  不删除）、凭据默认脱敏且明文只落本机 vault、分级轮换（用户归档默认 7 份，升级前 / 恢复前快照受保护）、
  升级 dsh / 插件前自动快照。
- **会话日志体检与定点修复**：zstd 帧结构 + 首帧契约 + seq 连续性三项判定；修复 = 从**已验证归档**
  把对应文件原样覆盖回来，损坏现场先留档 `*.corrupt-<时间戳>`；修复后 SHA256 与原始文件逐字节一致。
  新增第二条可自动修复规则 `DSH-SESSION-BATCH-031`。
- **插件兼容性预检**：`peerDependencies` 数值窗口 + `engines.node`；三种结论 `OK` / `FAIL` / **未知**，
  **未知绝不当通过**；安装 / 升级前强制拦截（`compatGateOnUpdate`，默认开）。
- **插件依赖三层解析**：插件自己的 `node_modules` → profile → 宿主 dsh，三层都找不到才算缺。
- **插件卸载**：插件管理页每张插件的按钮固定为 修复 / 升级 / 卸载；走官方
  `dsh plugin --profile web remove` 通道，二次确认 + 卸载前快照 + 复读校验（没卸掉不假报成功）；
  只有界面入口，不做批量卸载。
- **网络自适应**：配置代理 → 系统代理 → 环境变量 → 本机常见端口探测（只做回环），不污染子进程环境。
- **托盘常驻 + 单实例 + 崩溃日志**：关窗口最小化到托盘（`minimizeToTray`）；第二个实例把已有窗口
  唤到前台；未捕获异常写 `DSH管家-crash.log` 并继续运行。
- **诊断规则 11 → 19**：新增 8 条环境层规则（090 / 091 / 092 / 100 / 101 / 110 / 120 / 031），
  只用本机已有文件判定、不走网络。
- **新增命令行**：`--backup` / `--backups` / `--verify` / `--restore` / `--sessions` / `--compat` / `--versions`。
- **新增配置项**：`dshHomeDir`、`backupDir`、`backupKeep`、`backupRedact`、`backupRedactOn`、
  `backupStaleDays`、`preUpgradeSnapshot`、`compatGateOnUpdate`、`proxyUrl`、`autoProxy`、
  `minimizeToTray`、`sessionDoctorMax`、`restartAfterUpdate`、`openBrowserAfterRestart`（旧配置自动补默认值）。

修复：

- 定点修复报「归档里没有副本」但其实有：归档条目路径相对 `DSH_HOME`、会话体检结论相对 `sessions\`，
  两套口径不一致；统一换算后再比对（实测修好）。
- vault 里的明文凭据补不回来：恢复时误把「脱敏规则」当成「被剔除清单」；现在按 `.redacted.json`
  的真实剔除清单 ∪ 当前脱敏规则取凭据。
- 关窗口直接退出、托盘形同虚设：只认 `CloseReason.UserClosing` 会让程序自己发起的关闭绕过托盘。
- 备份中心日志区空白：窗口构造阶段的日志被丢掉；改为先缓冲、句柄就绪后回填。
- 「校验」列被截断（列宽 70 → 84）；镜像源全部失败时只报最后一个错（现在合并输出全部原因）。

验证：编译 0 error / 0 warning；`--selftest` 77 项全通过（含插件卸载 8 项）；`--health` 21 项；
备份 / 校验 / 恢复 / 会话体检 / 兼容性预检全流程实跑；真实环境 `--diagnose` 命中新规则
`DSH-PLUGIN-COMPAT-100` 且未影响 DSH 进程。

兼容性：文件名、配置项、启动方式不变；旧配置文件直接可用（新字段有默认值）；原有页面与按钮一个没删。

已知限制（有意为之）：备份只落本机磁盘；不自动删除任何用户数据；真实环境未执行 `--restore` 与
`--repair`（沙箱内跑过全流程）；真实环境未真的卸载过插件（界面走到二次确认即止）。

## 1.2.1 — 2026-09-23 — 已进入更新通道

**定位升级：从「体检器 + 更新器」变为「故障诊断中心 + 维护中心」。**

新增：

- **诊断链路**：受管启动 / 接管 DSH → 采集真实现场（stdout、stderr、端口、进程树、健康探针）→
  提取证据 → 命中 11 条诊断规则 → 结论必须带证据（输出片段、堆栈帧、端口占用者、文件路径 + 行号）。
- **唯一一条可自动修复的闭环规则** `DSH-JSON-BOM-001`：前置 SHA256 校验 → Recovery 备份 →
  剥离 BOM 3 字节 → Node `JSON.parse` 复验 → 重启 → 健康探针 → 提交或自动回滚。
- **维护隔离**：原「一键修复」降级为维护中心里的「全套维护」，诊断链路不再自动升级任何东西。
- **离线自测**：`--selftest` 在临时沙箱（假 DSH、假 npm）跑 69 项断言。
- 命令行新增 `--diagnose` / `--repair [--yes]` / `--selftest` / `--stop-dsh`。

修复：

- 健康地址缓存串场：旧实例淘汰的地址会污染新实例，导致误判「启动失败」；现在新会话一律作废旧地址，
  且缓存地址必须端口仍在监听才算数。
- 端口误报：管家自己启动的 DSH 正在监听被当成「端口故障」；现在属于正面证据，只有他人占用才提示。
- 多实例认错：机器上存在多个 dsh node 进程时只认第一个会认错；现在先列全部候选，再按进程树 /
  同一安装目录归属判断。
- 管道调用卡死：受管启动的 DSH 继承标准句柄，等于一直攥着调用方的管道写端（实测卡住 497 秒）；
  改用 `CreateProcess`（`bInheritHandles=FALSE`）后同一场景 49 秒正常返回。
- 配置路径越存越长：读配置未还原 JSON 转义，`C:\...` 读出来变成 `C:\\...`，再保存反斜杠翻倍；
  现在按 JSON 规则还原，并补了「存 → 读 → 再存 → 再读」回归断言。
- 自带日志文件头有 BOM：启动时一次性清除历史 BOM，新写日志一律无 BOM。
- 探针每秒刷日志：改为仅在地址或来源变化时记录一行。

兼容性：文件名、配置项、启动方式不变；旧配置文件可直接使用；`--health` 的输出结构与退出码与 1.1.0 一致。

说明文档：[`docs/diagnostics.md`](./docs/diagnostics.md)、[`docs/troubleshooting.md`](./docs/troubleshooting.md)。

## 1.1.0 — 2026-09-19 — 本地交付版（未进入更新通道）

- 重做图形界面：顶部操作栏 + 概览 / 插件管理 / 运行日志三个页面。
- 每个体检项目提供独立的「修复 / 升级」按钮，操作完成后自动重新体检。
- 自动检测读取 web profile 依赖清单，逐项列出插件名称、当前版本、最新版本与更新状态。
- 区分语义：「修复」按当前版本重装，「升级」才安装最新版本。

## 1.0.2 — 2026-09-19 — 已进入更新通道

- 修复「一键修复 / 立即更新」结束后的自动复检被防重入锁吞掉的问题。
- 修复配置 `checkUpdatesOnLaunch: false` 不生效的问题（新增布尔字段解析）。
- 修复预发布版本比较使用字符串排序导致 `rc.10` 被判为小于 `rc.9` 的问题（改为逐段语义化比较）。

## 1.0.1 — 本地修复批次（未作为独立版本进入更新通道）

- 日志每行写两遍；`DSH管家-health.json` 只在命令行模式更新；git 离线时长时间卡住；
  自更新失败原因被吞掉；报告结论自相矛盾；联网查询走 Schannel 连不上 CDN（改用 node/OpenSSL）。

## 1.0.0 — 2026-09-11 — 首个公开版本

- Windows 单文件程序：npm 镜像切换、全局 dsh 检查/安装/升级、插件版本对齐、仓库拉取与构建、
  配套服务启动、插件更新检查、官方 `doctor` 自检。
- 建立 jsDelivr CDN 更新通道（`manifest.json` + `DSH管家.bin`），程序内可自动检查与替换。
- 加固：stdin 重定向、`GIT_TERMINAL_PROMPT`、防重入、全插件检查。
